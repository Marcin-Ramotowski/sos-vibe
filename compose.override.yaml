services:
  app:
    build:
      target: development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      NODE_ENV: development
    ports:
      - "${APP_PORT:-3000}:3000"

  db:
    ports:
      - "${DB_PORT:-5432}:5432"
