# Archived MVP Planning Materials

This directory is a historical snapshot from the MVP build phase.

It is not a source of truth for current product, architecture, API, test, or implementation decisions.

Agents must not use files in this directory unless the user explicitly asks for historical MVP context.