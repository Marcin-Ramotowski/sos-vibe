# Documentation files — wget http://localhost in docker exec health checks
# are loopback connections inside containers; HTTP is intentional (TLS
# terminates at Cloudflare Tunnel, not inside Node.js).
**/*.md
.ai/
